name: "Test And Build Platform"

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

env:
  REV: ${{ github.run_id }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    strategy:
      matrix:
        os: [
          "macos-latest",
          "self-hosted-linux-arm",
          "self-hosted-linux-arm64",
          "ubuntu-latest",
        ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - name: Clean temp files
        run: rm -rf ${{ github.workspace }}/_temp
        if: ${{ matrix.os == 'self-hosted-linux-arm' }}
      - uses: ./.github/actions/setup
        with:
          gh-token: ${{ secrets.CC_BUILD_TOKEN }}
      - name: Unit Tests
        run: cargo test-all-features

  lint-code:
    strategy:
      matrix:
        os: [
          "macos-latest",
          "self-hosted-linux-arm",
          "self-hosted-linux-arm64",
          "ubuntu-latest",
        ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - name: Clean temp files
        run: rm -rf ${{ github.workspace }}/_temp
        if: ${{ matrix.os == 'self-hosted-linux-arm' }}
      - uses: ./.github/actions/setup
        with:
          gh-token: ${{ secrets.CC_BUILD_TOKEN }}
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check code
        run: cargo check-all-features
        # TODO(eric): once the rest of the code passes, enable
        # this for all crates.
      - name: Clippy
        run: cargo clippy -p crypto -- -D warnings

  build-release:
    strategy:
      matrix:
        os: [
          "macos-latest",
          "self-hosted-linux-arm",
          "self-hosted-linux-arm64",
          "ubuntu-latest",
        ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - name: Clean temp files
        run: rm -rf ${{ github.workspace }}/_temp
        if: ${{ matrix.os == 'self-hosted-linux-arm' }}
      - uses: ./.github/actions/setup
        with:
          gh-token: ${{ secrets.CC_BUILD_TOKEN }}
      - name: Build code
        run: cargo --verbose build-all-features --release
