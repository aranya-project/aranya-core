name: "Generate Docs"

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
  workflow_call:
    inputs:
      workflow:
        required: true
        type: string
    secrets:
      CC_BUILD_TOKEN:
        required: true
      GH_DOTCOM_TOKEN:
        required: true

env:
  REV: ${{ github.run_id }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.workflow }}
  cancel-in-progress: true

jobs:
  doc:
    runs-on: "self-hosted-linux-amd64"
    steps:
      # Our runners don't get wiped between runs :/
      - name: nuke old .gitconfig
        run: rm ~/.gitconfig || true
      - uses: actions/checkout@v3
      - uses: ./.github/actions/setup
        with:
          ghe-token: ${{ secrets.CC_BUILD_TOKEN }}
          gh-token: ${{ secrets.GH_DOTCOM_TOKEN }}
          os: "self-hosted-linux-amd64"
      - name: Install nightly
        # Pinned to avoid https://github.com/rust-lang/rust/issues/118670
        run: rustup toolchain install nightly-2023-12-04
      - name: Generate docs
        env:
          RUSTDOCFLAGS: "--cfg docs -Dwarnings"
        run: |
          if ! command -v zip; then
            sudo apt-get install -y zip \
              --no-install-recommends
          fi
          # Pinned to avoid https://github.com/rust-lang/rust/issues/118670
          cargo +nightly-2023-12-04 doc --all-features --workspace
          zip -qr docs.zip target/doc
      - name: Publish docs
        if: ${{ always() }}
        uses: actions/upload-artifact@v3
        with:
          name: docs
          path: docs.zip
          if-no-files-found: error
