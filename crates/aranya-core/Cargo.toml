[package]
name = "aranya-core"
description = "Stable public API for the Aranya runtime"
version = "0.0.1-alpha"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
aranya-crypto = { version = "0.14.0", default-features = false }
aranya-runtime = { version = "0.22.0", default-features = false }

[features]
default = []

libc = ["aranya-runtime/libc"]

std = [
	"aranya-crypto/std",
	"aranya-runtime/std",
]

low-mem-usage = ["aranya-runtime/low-mem-usage"]

[dev-dependencies]
anyhow = "1"
aranya-core = { path = ".", features = ["std", "libc"] }
aranya-crypto = { version = "0.14.0", features = ["std", "alloc", "getrandom", "memstore"] }
aranya-crypto-ffi = "0.21.0"
aranya-device-ffi = "0.21.0"
aranya-envelope-ffi = "0.21.0"
aranya-idam-ffi = "0.21.0"
aranya-perspective-ffi = "0.21.0"
aranya-policy-compiler = "0.22.0"
aranya-policy-lang = "0.14.1"
aranya-policy-vm = "0.21.0"
postcard = { workspace = true, features = ["alloc"] }

[[example]]
name = "simple"
path = "examples/simple.rs"
required-features = ["std", "libc"]
