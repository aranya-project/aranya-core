action test(n int) {
    if false {
        check 1 else test_fail("check failed")
    }

    if false {
        check 1 else test_fail("check failed")
    } else {
        check 2 else test_fail("check failed")
    }

    if false {
        check 1 else test_fail("check failed")
        let c = add(1, 1)
    } else if n == 2 {
        check 3 else test_fail("check failed")
    } else if n == 4 {
        check 5 else test_fail("check failed")
    } else {
        check 6 else test_fail("check failed")
    }
}
