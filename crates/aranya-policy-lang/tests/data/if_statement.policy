action test(n int) result[unit, string] {
    if false {
        check 1 else return Err("check failed")
    }

    if false {
        check 1 else return Err("check failed")
    } else {
        check 2 else return Err("check failed")
    }

    if false {
        check 1 else return Err("check failed")
        let c = add(1, 1)
    } else if n == 2 {
        check 3 else return Err("check failed")
    } else if n == 4 {
        check 5 else return Err("check failed")
    } else {
        check 6 else return Err("check failed")
    }
    return Ok(Unit)
}
