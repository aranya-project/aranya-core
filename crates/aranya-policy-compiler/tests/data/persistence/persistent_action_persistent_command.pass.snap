---
source: crates/aranya-policy-compiler/tests/tests.rs
expression: module
---
Module (version 0)

labels:
	policy:Cmd -> 1
	recall:Cmd -> 10
	seal:Cmd -> 15
	open:Cmd -> 22
	action:test -> 29

action_defs:
	test:
		persistence: persistent
		params:

command_defs:
	Cmd:
		persistence: persistent
		attributes:
		fields:

fact_defs:

struct_defs:
	Cmd:

enum_defs:

globals:

program memory:
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	meta: finish enabled
	block
	end
	exit normal
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	return
	meta: Set `this`
	def this
	save SP
	exit panic
	restore SP
	return
	exit panic
	meta: Set `envelope`
	def envelope
	save SP
	exit panic
	restore SP
	return
	exit panic
	struct.new Cmd
	publish
	return
