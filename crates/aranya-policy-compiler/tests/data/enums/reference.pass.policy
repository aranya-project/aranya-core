enum Result { OK, Err }
action test() result[unit, string] {
    let ok = Result::OK
    check ok == Result::OK else return Err("check failed")
    check ok != Result::Err else return Err("check failed")

    match ok {
        Result::OK => {}
        Result::Err => {}
    }
    return Ok(Unit)
}
