enum Result { OK, Err }
action test() {
    let ok = Result::OK
    check ok == Result::OK else todo()
    check ok != Result::Err else todo()

    match ok {
        Result::OK => {}
        Result::Err => {}
    }
}
