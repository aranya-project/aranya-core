---
source: crates/aranya-policy-compiler/tests/tests.rs
expression: module
---
Module (version 0)

labels:
	action:pets -> 1

action_defs:
	pets:
		persistence: persistent
		params:

command_defs:

fact_defs:
	Pet (mutable)
		key:
			name: string
		value:
			age: int

struct_defs:
	Pet:
		name: string
		age: int

enum_defs:

globals:

program memory:
	exit panic
	fact.new Pet
	query.start
	block
	query.next p
	branch 13
	meta: Get `p`
	get p
	struct.get age
	meta: Set `age`
	def age
	end
	jump 3
	end
	return
