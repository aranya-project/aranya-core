---
source: crates/aranya-policy-compiler/tests/tests.rs
expression: module
---
Module (version 0)

labels:
	policy:Foo -> 1
	recall:Foo -> 10
	seal:Foo -> 15
	open:Foo -> 22

action_defs:

command_defs:
	Foo:
		persistence: persistent
		attributes:
		fields:
			a: int
			b: string
			c: bool

fact_defs:

struct_defs:
	Bar:
		a: int
	Baz:
		a: int
		b: string
	Foo:
		a: int
		b: string
		c: bool

enum_defs:

globals:

program memory:
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	meta: finish enabled
	block
	end
	exit normal
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	return
	meta: Set `this`
	def this
	save SP
	exit panic
	restore SP
	return
	exit panic
	meta: Set `envelope`
	def envelope
	save SP
	exit panic
	restore SP
	return
	exit panic
