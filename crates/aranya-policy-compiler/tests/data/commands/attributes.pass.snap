---
source: crates/aranya-policy-compiler/tests/tests.rs
expression: module
---
Module (version 0)

labels:
	policy:A -> 1
	recall:A -> 10
	seal:A -> 15
	open:A -> 22

action_defs:

command_defs:
	A:
		persistence: persistent
		attributes:
			i: 5
			s: "abc"
			priority: Priority::1
		fields:

fact_defs:

struct_defs:
	A:

enum_defs:
	Priority:
		High = 1
		Low = 0

globals:

program memory:
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	meta: finish enabled
	block
	end
	exit normal
	exit panic
	meta: Set `envelope`
	def envelope
	meta: Set `this`
	def this
	return
	meta: Set `this`
	def this
	save SP
	exit panic
	restore SP
	return
	exit panic
	meta: Set `envelope`
	def envelope
	save SP
	exit panic
	restore SP
	return
	exit panic
