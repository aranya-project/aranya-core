fact Pet[name string]=>{age int}
action pets() result[unit, string] {
    let n = 42
    map Pet[name:?] as p {
        check p.age > 0 else return Err("check failed")
        check n == 42 else return Err("check failed")
    }
    return Ok(Unit)
}
