---
source: crates/aranya-policy-compiler/tests/tests.rs
expression: module
---
Module (version 0)

labels:
	action:bar -> 1
	action:foo -> 6

action_defs:
	bar:
		persistence: persistent
		params:
			n: int
			s: string
	foo:
		persistence: persistent
		params:

command_defs:

fact_defs:

struct_defs:

enum_defs:

globals:

program memory:
	exit panic
	meta: Set `s`
	def s
	meta: Set `n`
	def n
	return
	const 1
	const "abc"
	call 1
	return
