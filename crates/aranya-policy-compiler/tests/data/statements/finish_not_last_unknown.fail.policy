fact Blah[] => {}
command Foo {
    fields {}
    seal { return todo() }
    open { return todo() }
    policy {
        check true else recall test_fail("should pass")
        finish {
            delete Blah[]
        } // finish must be the last statement in policy
        finish {
            delete Blah[]
        }
        let a = 5
    }
}
