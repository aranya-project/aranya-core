[package]
name = "canary-alloc"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
aranya-base58 = { path = "../../crates/aranya-base58" }
aranya-buggy = { path = "../../crates/aranya-buggy" }
# `aranya-crypto` enables `getrandom` by default.
aranya-crypto = { path = "../../crates/aranya-crypto", default-features = false }
aranya-trouble = { path = "../../crates/aranya-trouble" }
